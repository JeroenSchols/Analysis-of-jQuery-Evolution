[{"id":"d2bf489e1fbbf70423904906e9e34acf048bb828","instances":[{"path":"./jquery-data/3.0.0/src/ajax.js","lines":[560,562],"code":"if ( s.data && s.processData && typeof s.data !== \"string\" ) {\n\ts.data = jQuery.param( s.data, s.traditional );\n}"},{"path":"./jquery-data/1.4/src/ajax.js","lines":[212,214],"code":"if ( s.data && s.processData && typeof s.data !== \"string\" ) {\n\ts.data = jQuery.param( s.data, s.traditional );\n}"}]},
{"id":"65d8a8b792bf4313913fa27259900548f1da8ccb","instances":[{"path":"./jquery-data/3.0.0/src/core.js","lines":[148,169],"code":"if ( i === length ) {\n\ttarget = this;\n\ti--;\n}\n\nfor ( ; i < length; i++ ) {\n\n\t// Only deal with non-null/undefined values\n\tif ( ( options = arguments[ i ] ) != null ) {\n\n\t\t// Extend the base object\n\t\tfor ( name in options ) {\n\t\t\tsrc = target[ name ];\n\t\t\tcopy = options[ name ];\n\n\t\t\t// Prevent never-ending loop\n\t\t\tif ( target === copy ) {\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\t// Recurse if we're merging plain objects or arrays\n\t\t\tif ( deep && copy && ( jQuery.isPlainObject( copy ) ||"},{"path":"./jquery-data/1.4/src/core.js","lines":[301,320],"code":"if ( length === i ) {\n\ttarget = this;\n\t--i;\n}\n\nfor ( ; i < length; i++ ) {\n\t// Only deal with non-null/undefined values\n\tif ( (options = arguments[ i ]) != null ) {\n\t\t// Extend the base object\n\t\tfor ( name in options ) {\n\t\t\tsrc = target[ name ];\n\t\t\tcopy = options[ name ];\n\n\t\t\t// Prevent never-ending loop\n\t\t\tif ( target === copy ) {\n\t\t\t\tcontinue;\n\t\t\t}\n\n\t\t\t// Recurse if we're merging object literal values or arrays\n\t\t\tif ( deep && copy && ( jQuery.isPlainObject(copy) || jQuery.isArray(copy) ) ) {"}]},
{"id":"b065434b7d2f8647433eb86211e74c92fbdea584","instances":[{"path":"./jquery-data/3.0.0/src/effects.js","lines":[442,447],"code":"var opt = speed && typeof speed === \"object\" ? jQuery.extend( {}, speed ) : {\n\tcomplete: fn || !fn && easing ||\n\t\tjQuery.isFunction( speed ) && speed,\n\tduration: speed,\n\teasing: fn && easing || easing && !jQuery.isFunction( easing ) && easing\n};"},{"path":"./jquery-data/1.4/src/effects.js","lines":[239,244],"code":"var opt = speed && typeof speed === \"object\" ? speed : {\n\tcomplete: fn || !fn && easing ||\n\t\tjQuery.isFunction( speed ) && speed,\n\tduration: speed,\n\teasing: fn && easing || easing && !jQuery.isFunction(easing) && easing\n};"}]},
{"id":"4ebf6afe4a85b6fd504cbb356b0b845aa6628a1e","instances":[{"path":"./jquery-data/3.0.0/src/event.js","lines":[150,158],"code":"types = ( types || \"\" ).match( rnotwhite ) || [ \"\" ];\nt = types.length;\nwhile ( t-- ) {\n\ttmp = rtypenamespace.exec( types[ t ] ) || [];\n\ttype = origType = tmp[ 1 ];\n\tnamespaces = ( tmp[ 2 ] || \"\" ).split( \".\" ).sort();\n\n\t// There *must* be a type, no attaching namespace-only handlers\n\tif ( !type ) {"},{"path":"./jquery-data/3.0.0/src/event.js","lines":[232,240],"code":"types = ( types || \"\" ).match( rnotwhite ) || [ \"\" ];\nt = types.length;\nwhile ( t-- ) {\n\ttmp = rtypenamespace.exec( types[ t ] ) || [];\n\ttype = origType = tmp[ 1 ];\n\tnamespaces = ( tmp[ 2 ] || \"\" ).split( \".\" ).sort();\n\n\t// Unbind all events (on this namespace, if provided) for the element\n\tif ( !type ) {"}]},
{"id":"cb5299dcb3632ed75b2a272e625a741ca97fa0b7","instances":[{"path":"./jquery-data/3.0.0/src/manipulation.js","lines":[333,337],"code":"append: function() {\n\treturn domManip( this, arguments, function( elem ) {\n\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\tvar target = manipulationTarget( this, elem );\n\t\t\ttarget.appendChild( elem );"},{"path":"./jquery-data/3.0.0/src/manipulation.js","lines":[342,346],"code":"prepend: function() {\n\treturn domManip( this, arguments, function( elem ) {\n\t\tif ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n\t\t\tvar target = manipulationTarget( this, elem );\n\t\t\ttarget.insertBefore( elem, target.firstChild );"}]},
{"id":"fd4e1c9c853e11db1f12e7f324f8d807c12b208c","instances":[{"path":"./jquery-data/3.0.0/src/manipulation.js","lines":[452,460],"code":"jQuery.each( {\n\tappendTo: \"append\",\n\tprependTo: \"prepend\",\n\tinsertBefore: \"before\",\n\tinsertAfter: \"after\",\n\treplaceAll: \"replaceWith\"\n}, function( name, original ) {\n\tjQuery.fn[ name ] = function( selector ) {\n\t\tvar elems,"},{"path":"./jquery-data/1.4/src/manipulation.js","lines":[353,361],"code":"jQuery.each({\n\tappendTo: \"append\",\n\tprependTo: \"prepend\",\n\tinsertBefore: \"before\",\n\tinsertAfter: \"after\",\n\treplaceAll: \"replaceWith\"\n}, function( name, original ) {\n\tjQuery.fn[ name ] = function( selector ) {\n\t\tvar ret = [], insert = jQuery( selector );"}]},
{"id":"2691372d4fae4363f7385195b0216a31bccfe8b0","instances":[{"path":"./jquery-data/3.0.0/src/serialize.js","lines":[111,117],"code":".map( function( i, elem ) {\n\tvar val = jQuery( this ).val();\n\n\treturn val == null ?\n\t\tnull :\n\t\tjQuery.isArray( val ) ?\n\t\t\tjQuery.map( val, function( val ) {"},{"path":"./jquery-data/1.4/src/ajax.js","lines":[94,100],"code":".map(function( i, elem ) {\n\tvar val = jQuery(this).val();\n\n\treturn val == null ?\n\t\tnull :\n\t\tjQuery.isArray(val) ?\n\t\t\tjQuery.map( val, function( val, i ) {"}]},
{"id":"9e273ef6d6e4bf09d8cf4178317debc2e7babbb7","instances":[{"path":"./jquery-data/3.0.0/src/traversing.js","lines":[110,116],"code":"jQuery.each( {\n\tparent: function( elem ) {\n\t\tvar parent = elem.parentNode;\n\t\treturn parent && parent.nodeType !== 11 ? parent : null;\n\t},\n\tparents: function( elem ) {\n\t\treturn dir( elem, \"parentNode\" );"},{"path":"./jquery-data/1.4/src/traversing.js","lines":[165,171],"code":"jQuery.each({\n\tparent: function( elem ) {\n\t\tvar parent = elem.parentNode;\n\t\treturn parent && parent.nodeType !== 11 ? parent : null;\n\t},\n\tparents: function( elem ) {\n\t\treturn jQuery.dir( elem, \"parentNode\" );"}]},
{"id":"e369af50c679e66e303896a061a2c7a992bdc6cd","instances":[{"path":"./jquery-data/3.0.0/src/wrap.js","lines":[47,60],"code":"\treturn this.each( function() {\n\t\tvar self = jQuery( this ),\n\t\t\tcontents = self.contents();\n\n\t\tif ( contents.length ) {\n\t\t\tcontents.wrapAll( html );\n\n\t\t} else {\n\t\t\tself.append( html );\n\t\t}\n\t} );\n},\n\nwrap: function( html ) {"},{"path":"./jquery-data/1.4/src/manipulation.js","lines":[79,91],"code":"\treturn this.each(function() {\n\t\tvar self = jQuery( this ), contents = self.contents();\n\n\t\tif ( contents.length ) {\n\t\t\tcontents.wrapAll( html );\n\n\t\t} else {\n\t\t\tself.append( html );\n\t\t}\n\t});\n},\n\nwrap: function( html ) {"}]},
{"id":"b12f113a83f0008c2101e97e8ab617107103809a","instances":[{"path":"./jquery-data/3.0.0/src/attributes/attr.js","lines":[27,37],"code":"attr: function( elem, name, value ) {\n\tvar ret, hooks,\n\t\tnType = elem.nodeType;\n\n\t// Don't get/set attributes on text, comment and attribute nodes\n\tif ( nType === 3 || nType === 8 || nType === 2 ) {\n\t\treturn;\n\t}\n\n\t// Fallback to prop when attributes are not supported\n\tif ( typeof elem.getAttribute === \"undefined\" ) {"},{"path":"./jquery-data/3.0.0/src/attributes/prop.js","lines":[26,35],"code":"prop: function( elem, name, value ) {\n\tvar ret, hooks,\n\t\tnType = elem.nodeType;\n\n\t// Don't get/set properties on text, comment and attribute nodes\n\tif ( nType === 3 || nType === 8 || nType === 2 ) {\n\t\treturn;\n\t}\n\n\tif ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {"}]},
{"id":"fa35d943bb563a4d7230fc3e31d0cdb2b7fb96f2","instances":[{"path":"./jquery-data/3.0.0/src/attributes/classes.js","lines":[17,23],"code":"addClass: function( value ) {\n\tvar classes, elem, cur, curValue, clazz, j, finalValue,\n\t\ti = 0;\n\n\tif ( jQuery.isFunction( value ) ) {\n\t\treturn this.each( function( j ) {\n\t\t\tjQuery( this ).addClass( value.call( this, j, getClass( this ) ) );"},{"path":"./jquery-data/3.0.0/src/attributes/classes.js","lines":[55,61],"code":"removeClass: function( value ) {\n\tvar classes, elem, cur, curValue, clazz, j, finalValue,\n\t\ti = 0;\n\n\tif ( jQuery.isFunction( value ) ) {\n\t\treturn this.each( function( j ) {\n\t\t\tjQuery( this ).removeClass( value.call( this, j, getClass( this ) ) );"}]},
{"id":"c80935bb1052c2705c6b2f903ff15d80054ec4d4","instances":[{"path":"./jquery-data/3.0.0/src/attributes/classes.js","lines":[27,37],"code":"if ( typeof value === \"string\" && value ) {\n\tclasses = value.match( rnotwhite ) || [];\n\n\twhile ( ( elem = this[ i++ ] ) ) {\n\t\tcurValue = getClass( elem );\n\t\tcur = elem.nodeType === 1 &&\n\t\t\t( \" \" + curValue + \" \" ).replace( rclass, \" \" );\n\n\t\tif ( cur ) {\n\t\t\tj = 0;\n\t\t\twhile ( ( clazz = classes[ j++ ] ) ) {"},{"path":"./jquery-data/3.0.0/src/attributes/classes.js","lines":[69,81],"code":"if ( typeof value === \"string\" && value ) {\n\tclasses = value.match( rnotwhite ) || [];\n\n\twhile ( ( elem = this[ i++ ] ) ) {\n\t\tcurValue = getClass( elem );\n\n\t\t// This expression is here for better compressibility (see addClass)\n\t\tcur = elem.nodeType === 1 &&\n\t\t\t( \" \" + curValue + \" \" ).replace( rclass, \" \" );\n\n\t\tif ( cur ) {\n\t\t\tj = 0;\n\t\t\twhile ( ( clazz = classes[ j++ ] ) ) {"}]},
{"id":"738f991e7abd7dbbeb2d5db8968aa9275bcc1dda","instances":[{"path":"./jquery-data/3.0.0/src/core/ready-no-deferred.js","lines":[23,60],"code":"\twhenReady( fn );\n\treturn this;\n};\n\njQuery.extend( {\n\n\t// Is the DOM ready to be used? Set to true once it occurs.\n\tisReady: false,\n\n\t// A counter to track how many items to wait for before\n\t// the ready event fires. See #6781\n\treadyWait: 1,\n\n\t// Hold (or release) the ready event\n\tholdReady: function( hold ) {\n\t\tif ( hold ) {\n\t\t\tjQuery.readyWait++;\n\t\t} else {\n\t\t\tjQuery.ready( true );\n\t\t}\n\t},\n\n\tready: function( wait ) {\n\n\t\t// Abort if there are pending holds or we're already ready\n\t\tif ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Remember that the DOM is ready\n\t\tjQuery.isReady = true;\n\n\t\t// If a normal DOM Ready event fired, decrement, and wait if need be\n\t\tif ( wait !== true && --jQuery.readyWait > 0 ) {\n\t\t\treturn;\n\t\t}\n\n\t\twhenReady = function( fn ) {"},{"path":"./jquery-data/3.0.0/src/core/ready.js","lines":[14,56],"code":"\treadyList.then( fn );\n\n\treturn this;\n};\n\njQuery.extend( {\n\n\t// Is the DOM ready to be used? Set to true once it occurs.\n\tisReady: false,\n\n\t// A counter to track how many items to wait for before\n\t// the ready event fires. See #6781\n\treadyWait: 1,\n\n\t// Hold (or release) the ready event\n\tholdReady: function( hold ) {\n\t\tif ( hold ) {\n\t\t\tjQuery.readyWait++;\n\t\t} else {\n\t\t\tjQuery.ready( true );\n\t\t}\n\t},\n\n\t// Handle when the DOM is ready\n\tready: function( wait ) {\n\n\t\t// Abort if there are pending holds or we're already ready\n\t\tif ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Remember that the DOM is ready\n\t\tjQuery.isReady = true;\n\n\t\t// If a normal DOM Ready event fired, decrement, and wait if need be\n\t\tif ( wait !== true && --jQuery.readyWait > 0 ) {\n\t\t\treturn;\n\t\t}\n\n\t\t// If there are functions bound, to execute\n\t\treadyList.resolveWith( document, [ jQuery ] );\n\t}\n} );"}]},
{"id":"e715059f755554122e48924d7088fc8680794194","instances":[{"path":"./jquery-data/3.0.0/src/queue/delay.js","lines":[11,15],"code":"jQuery.fn.delay = function( time, type ) {\n\ttime = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;\n\ttype = type || \"fx\";\n\n\treturn this.queue( type, function( next, hooks ) {"},{"path":"./jquery-data/1.4/src/queue.js","lines":[75,79],"code":"delay: function( time, type ) {\n\ttime = jQuery.fx ? jQuery.fx.speeds[time] || time : time;\n\ttype = type || \"fx\";\n\n\treturn this.queue( type, function() {"}]},
{"id":"7a199e64926353b47675f1e5683d8c2139ba4f53","instances":[{"path":"./jquery-data/1.4/src/attributes.js","lines":[32,38],"code":"if ( value && typeof value === \"string\" ) {\n\tvar classNames = (value || \"\").split( rspace );\n\n\tfor ( var i = 0, l = this.length; i < l; i++ ) {\n\t\tvar elem = this[i];\n\n\t\tif ( elem.nodeType === 1 ) {"},{"path":"./jquery-data/1.4/src/attributes.js","lines":[65,71],"code":"if ( (value && typeof value === \"string\") || value === undefined ) {\n\tvar classNames = (value || \"\").split(rspace);\n\n\tfor ( var i = 0, l = this.length; i < l; i++ ) {\n\t\tvar elem = this[i];\n\n\t\tif ( elem.nodeType === 1 && elem.className ) {"}]},
{"id":"620eca1e3ae37fec2f4ba87fdc57a53a3cce4302","instances":[{"path":"./jquery-data/1.4/src/data.js","lines":[17,26],"code":"data: function( elem, name, data ) {\n\tif ( elem.nodeName && jQuery.noData[elem.nodeName.toLowerCase()] ) {\n\t\treturn;\n\t}\n\n\telem = elem == window ?\n\t\twindowData :\n\t\telem;\n\n\tvar id = elem[ expando ], cache = jQuery.cache, thisCache;"},{"path":"./jquery-data/1.4/src/data.js","lines":[60,69],"code":"removeData: function( elem, name ) {\n\tif ( elem.nodeName && jQuery.noData[elem.nodeName.toLowerCase()] ) {\n\t\treturn;\n\t}\n\n\telem = elem == window ?\n\t\twindowData :\n\t\telem;\n\n\tvar id = elem[ expando ], cache = jQuery.cache, thisCache = cache[ id ];"}]},
{"id":"6515a08f3775a080fb835c826731d8b6dcd3fed5","instances":[{"path":"./jquery-data/1.4/src/manipulation.js","lines":[121,124],"code":"before: function() {\n\tif ( this[0] && this[0].parentNode ) {\n\t\treturn this.domManip(arguments, false, function( elem ) {\n\t\t\tthis.parentNode.insertBefore( elem, this );"},{"path":"./jquery-data/1.4/src/manipulation.js","lines":[133,136],"code":"after: function() {\n\tif ( this[0] && this[0].parentNode ) {\n\t\treturn this.domManip(arguments, false, function( elem ) {\n\t\t\tthis.parentNode.insertBefore( elem, this.nextSibling );"}]},
{"id":"ced40f8cd9d8a26e41c7a814a1ac93f15c35ae11","instances":[{"path":"./jquery-data/1.4/src/offset.js","lines":[1,16],"code":"if ( \"getBoundingClientRect\" in document.documentElement ) {\n\tjQuery.fn.offset = function( options ) {\n\t\tvar elem = this[0];\n\n\t\tif ( !elem || !elem.ownerDocument ) {\n\t\t\treturn null;\n\t\t}\n\n\t\tif ( options ) { \n\t\t\treturn this.each(function( i ) {\n\t\t\t\tjQuery.offset.setOffset( this, options, i );\n\t\t\t});\n\t\t}\n\n\t\tif ( elem === elem.ownerDocument.body ) {\n\t\t\treturn jQuery.offset.bodyOffset( elem );"},{"path":"./jquery-data/1.4/src/offset.js","lines":[27,42],"code":"} else {\n\tjQuery.fn.offset = function( options ) {\n\t\tvar elem = this[0];\n\n\t\tif ( !elem || !elem.ownerDocument ) {\n\t\t\treturn null;\n\t\t}\n\n\t\tif ( options ) { \n\t\t\treturn this.each(function( i ) {\n\t\t\t\tjQuery.offset.setOffset( this, options, i );\n\t\t\t});\n\t\t}\n\n\t\tif ( elem === elem.ownerDocument.body ) {\n\t\t\treturn jQuery.offset.bodyOffset( elem );"}]}]
